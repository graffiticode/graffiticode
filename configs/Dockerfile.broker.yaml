FROM node:22-alpine

WORKDIR /usr/src/app

ENV NODE_ENV=production

# git: learnosity-sdk-nodejs is installed from GitHub.
RUN apk add --no-cache git
COPY . .
RUN npm ci --omit=dev

EXPOSE 8080
CMD [ "node", "packages/broker/src/main.js" ]
